import {_local} from './local';


const workplace = {
    commissionList: `${_local}/remindController/commissionList`,        //待办事项
    documentTypeList: `${_local}/StaticDataController/documentTypeList`,        //单据类型和 最新单据列表
}

export default workplace;
